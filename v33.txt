Table Name Instruction:

Whenever referring to a table in the user_query, always use the actual table name (as provided in the mapping), not a value or description.

Example: write "customer_interaction table" or "transcript_insight table" instead of "customer calls" or "transcript data".

This ensures the text2SQL pipeline can correctly map natural-language instructions to the real table.
